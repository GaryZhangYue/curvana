% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{crossing_x0}
\alias{crossing_x0}
\title{Interpolate the x-intercept (x0) where y = 0}
\usage{
crossing_x0(x1, y1, x2, y2)
}
\arguments{
\item{x1}{Numeric. The x-coordinate of the first point.}

\item{y1}{Numeric. The y-coordinate of the first point.}

\item{x2}{Numeric. The x-coordinate of the second point.}

\item{y2}{Numeric. The y-coordinate of the second point.}
}
\value{
A single numeric value for the interpolated \code{x0} coordinate.
Returns \code{NA} if no valid sign change exists between \code{y1} and \code{y2}.
}
\description{
Performs linear interpolation between two points \code{(x1, y1)} and
\code{(x2, y2)} to find the x-coordinate (\code{x0}) where the line
crosses the x-axis (\code{y = 0}). Only valid when \code{y1} and \code{y2}
have opposite signs.
}
\examples{
crossing_x0(0, 4, 2, -2)  # 1.333 — line crosses y=0 between x=0 and x=2
crossing_x0(0, 4, 2, 6)   # NA — no sign change

}
\seealso{
[area_trapezoid()], [area_triangle()], [calculate_area()]
}
