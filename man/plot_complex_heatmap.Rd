% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{plot_complex_heatmap}
\alias{plot_complex_heatmap}
\title{Plot a scaled feature heatmap using ComplexHeatmap}
\usage{
plot_complex_heatmap(
  df,
  include_columns,
  annotate_columns = NULL,
  annotation_colors = NULL,
  cluster_rows = TRUE,
  cluster_columns = TRUE,
  show_row_dend = TRUE,
  show_column_dend = TRUE,
  show_row_names = TRUE,
  show_column_names = TRUE,
  heatmap_name = "z-score",
  row_title = "Features",
  column_title = NULL,
  draw = TRUE
)
}
\arguments{
\item{df}{A data.frame containing feature and metadata columns. Each row is
one sample.}

\item{include_columns}{Character vector of feature column names to include as
heatmap rows.}

\item{annotate_columns}{Optional character vector (length 0 to 2) of metadata
column names used to annotate heatmap columns (samples).}

\item{annotation_colors}{Optional named list of color mappings for annotation
columns passed to \code{ComplexHeatmap::HeatmapAnnotation(col = ...)}.}

\item{cluster_rows}{Logical; whether to cluster heatmap rows.}

\item{cluster_columns}{Logical; whether to cluster heatmap columns.}

\item{show_row_dend}{Logical; whether to display the row dendrogram.}

\item{show_column_dend}{Logical; whether to display the column dendrogram.}

\item{show_row_names}{Logical; whether to display row names (feature names).}

\item{show_column_names}{Logical; whether to display column names (sample names).}

\item{heatmap_name}{Character legend title for the scaled values.}

\item{row_title}{Optional row title.}

\item{column_title}{Optional column title.}

\item{draw}{Logical; if \code{TRUE}, draws the heatmap immediately.}
}
\value{
A \code{ComplexHeatmap} object. When \code{draw = TRUE}, returns the
  object produced by \code{ComplexHeatmap::draw()}.
}
\description{
Creates a heatmap where selected feature columns are shown as rows and
samples are shown as columns. Feature values are z-score scaled by row
(feature) before plotting. Optionally annotates sample columns with up to
two metadata columns.
}
