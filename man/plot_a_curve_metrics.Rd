% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{plot_a_curve_metrics}
\alias{plot_a_curve_metrics}
\title{Plot one transformed curve with optional metadata annotations}
\usage{
plot_a_curve_metrics(
  fdobj,
  curve_name,
  useCurve = c("retract", "approach"),
  plot_raw = FALSE,
  annotate_noiseBand = TRUE,
  annotate_repulsive_energy = TRUE,
  annotate_adhesive_energy = TRUE,
  annotate_adhesive_force = TRUE,
  annotate_rupture_distance = TRUE,
  annotate_repulsive_distance = TRUE,
  xlim = NULL,
  ylim = NULL,
  title = NULL,
  base_size = 14,
  line_color = "grey35",
  point_color = "grey35",
  line_size = 0.7,
  point_size = 1.8,
  line_alpha = 0.9,
  point_alpha = 0.8,
  annotation_text_size = 3.3,
  interaction_label_color_rupture = "purple4",
  interaction_label_color_repulsive = "darkorange3",
  interaction_label_fill = "white",
  ...
)
}
\arguments{
\item{fdobj}{An object of class \code{fdObj}.}

\item{curve_name}{Character. Name of one curve to plot. Must match
\code{rownames(fdobj@metadata)}.}

\item{useCurve}{Character. Either \code{"retract"} or \code{"approach"}.}

\item{plot_raw}{Logical. If \code{TRUE}, also plot the corresponding raw curve
and return a two-column panel using \code{cowplot::plot_grid()}.}

\item{annotate_noiseBand}{Logical. If \code{TRUE}, annotate noise band from
\code{noiseBand_low_nN_<useCurve>} and \code{noiseBand_high_nN_<useCurve>},
and add "Noise band" labels above and below the band boundaries.}

\item{annotate_repulsive_energy}{Logical. If \code{TRUE}, add repulsive
energy value to subtitle when available.}

\item{annotate_adhesive_energy}{Logical. If \code{TRUE}, add adhesive 
energy value to subtitle when available.}

\item{annotate_adhesive_force}{Logical. If \code{TRUE}, annotate adhesive force
point and separation using arrow + label.}

\item{annotate_rupture_distance}{Logical. If \code{TRUE}, annotate rupture
distance/threshold using arrow + label.}

\item{annotate_repulsive_distance}{Logical. If \code{TRUE}, annotate repulsive
distance/threshold using arrow + label.}

\item{xlim}{Optional numeric vector of length 2 for x-axis limits.}

\item{ylim}{Optional numeric vector of length 2 for y-axis limits.}

\item{title}{Character. Plot title.}

\item{base_size}{Numeric. Base theme text size.}

\item{line_color}{Character. Line color for curves.}

\item{point_color}{Character. Point color for curves.}

\item{line_size}{Numeric. Line width for curves.}

\item{point_size}{Numeric. Point size for curves.}

\item{line_alpha}{Numeric. Line alpha for curves.}

\item{point_alpha}{Numeric. Point alpha for curves.}

\item{annotation_text_size}{Numeric. Text size used for annotation labels.}

\item{interaction_label_color_rupture}{Character. Text/line color for rupture
annotation label.}

\item{interaction_label_color_repulsive}{Character. Text/line color for
repulsive annotation label.}

\item{interaction_label_fill}{Character. Fill color for interaction label box.}

\item{...}{Additional ggplot2 components to add to the transformed-curve plot
(e.g., \code{theme()}, \code{scale_*()}, \code{coord_*()}, \code{labs()},
or extra \code{geom_*()} layers).}
}
\value{
A ggplot2 object, or a cowplot grid object when \code{plot_raw = TRUE}.
}
\description{
Takes an \code{fdObj}, the name of one curve, and the selected transformed
branch (approach or retract) as input, then builds a detailed visualization
of that single transformed force-distance curve. The function can annotate
noise-band bounds, adhesive force, adhesive and repulsive energies, rupture
distance, repulsive distance, and region labels derived from stored metadata.
When \code{plot_raw = TRUE}, it also plots the matching raw curve and returns
a two-panel display. The output is therefore either one annotated
\code{ggplot2} object or a combined cowplot panel containing both raw and
transformed views.
}
\examples{
folder <- system.file("extdata", package = "curvana")
fd_obj <- createFdObjFromFolder(folder)
fd_obj <- transform_curves(fd_obj, spring_constant = 0.1, useCurve = "retract", threads = 1, least_length= 300)
fd_obj <- analyze_curves_all_analytical_metrics(fd_obj, useCurve = "retract") 
plot_a_curve_metrics(fd_obj, curve_name = 'F_1.spm-F4E1_ForceCurveIndex_1.spm', useCurve = "retract", plot_raw = TRUE)
}
