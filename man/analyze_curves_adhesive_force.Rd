% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curve_analysis.R
\name{analyze_curves_adhesive_force}
\alias{analyze_curves_adhesive_force}
\title{Adhesive Force for All Transformed Curves in an fdObj}
\usage{
analyze_curves_adhesive_force(fdObj, useCurve = "retract", threads = 1)
}
\arguments{
\item{fdObj}{An object of class \code{fdObj}.}

\item{useCurve}{Character. Either "approach" or "retract" (default "retract").}

\item{threads}{Integer. Number of parallel workers (default 1).}
}
\value{
The updated \code{fdObj}.
}
\description{
Applies \code{analyze_a_curve_adhesive_force()} to each transformed curve in an \code{fdObj}
and stores results in metadata:
\itemize{
  \item \code{adhesive_force_nN_<dir>}
  \item \code{adhesive_sep_nm_<dir>}
}
}
\details{
Where \code{<dir>} is "approach" or "retract".

Behavior:
\itemize{
  \item If a curve is present and valid but has no negative force, force = 0 and sep = NA.
  \item If a curve cannot be analyzed at all (missing/invalid transformed data), both are NA.
}
}
