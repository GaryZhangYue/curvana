% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{plot_metric_violin}
\alias{plot_metric_violin}
\title{Plot one metric as a violin plot}
\usage{
plot_metric_violin(
  df,
  metric_name,
  group_by,
  color_by = NULL,
  color_map = NULL,
  facet_by = NULL,
  global_test = c("none", "anova", "kruskal"),
  pairwise_test = c("none", "t.test", "wilcox"),
  pairwise_comparisons = NULL,
  p_adjust_method = "BH",
  log10 = FALSE,
  add_points = TRUE,
  show_whisker_box = FALSE,
  add_smooth = FALSE,
  point_alpha = 0.6,
  point_color = "black",
  point_size = 1.2,
  jitter_width = 0.15,
  violin_fill = "grey85",
  violin_color = "grey40",
  violin_alpha = 0.8,
  smooth_color = "steelblue",
  base_size = 10
)
}
\arguments{
\item{df}{A data.frame containing grouping and metric columns. Typically the metadata slot of fdobj.}

\item{metric_name}{Character scalar. One column name in \code{df} to plot
on the y-axis.}

\item{group_by}{Character scalar. Column name in \code{df} used for the
x-axis grouping.}

\item{color_by}{Character scalar. Column name in \code{df} used to color
violins and points. Defaults to \code{group_by} when \code{NULL}.}

\item{color_map}{Optional named character vector of colors for groups in
\code{color_by} (e.g., \code{c(A = "#1b9e77", B = "#d95f02")}).}

\item{facet_by}{Character vector of length 0 to 2 specifying facet columns.
Use \code{NULL} for no faceting.}

\item{global_test}{Character scalar. One of \code{"none"}, \code{"anova"},
or \code{"kruskal"}. When not \code{"none"}, the global test p-value is
added as subtitle.}

\item{pairwise_test}{Character scalar. One of \code{"none"},
\code{"t.test"}, or \code{"wilcox"}. Pairwise comparisons are shown as
significance asterisks.}

\item{pairwise_comparisons}{Optional list of group-name pairs (e.g.,
\code{list(c("A", "B"), c("A", "C"))}). If \code{NULL}, all pairwise
combinations are used.}

\item{p_adjust_method}{Character p-value adjustment method passed to
\code{ggpubr::stat_compare_means()}.}

\item{log10}{Logical. If \code{TRUE}, applies \code{log10} transform to the
metric values before plotting and testing. Non-positive values are removed.}

\item{add_points}{Logical. If \code{TRUE}, overlays jittered points.}

\item{show_whisker_box}{Logical. If \code{TRUE}, overlays a whisker boxplot
on top of each violin.}

\item{add_smooth}{Logical. If \code{TRUE}, overlays \code{geom_smooth(method = "lm")}
within each facet.}

\item{point_alpha}{Numeric point transparency.}

\item{point_color}{Character point color.}

\item{point_size}{Numeric point size.}

\item{jitter_width}{Numeric horizontal jitter width.}

\item{violin_fill}{Character fill color for violins.}

\item{violin_color}{Character outline color for violins.}

\item{violin_alpha}{Numeric violin transparency.}

\item{smooth_color}{Character color for linear trend line.}

\item{base_size}{Numeric base font size for theme.}
}
\value{
A ggplot object.
}
\description{
Takes a regular data frame as input together with the name of one numeric
metric column and one grouping column, then constructs a violin plot for that
metric across groups. Optional inputs can add color grouping, faceting,
jittered points, boxplots, a linear trend line, and statistical summaries
such as a global ANOVA/Kruskal-Wallis test or pairwise t-test/Wilcoxon
comparisons. The output is a \code{ggplot} object that summarizes the
distribution of one metric across user-defined groups.
}
\examples{
folder <- system.file("extdata", package = "curvana")
fd_obj <- createFdObjFromFolder(folder)
fd_obj <- transform_curves(fd_obj, spring_constant = 0.1, useCurve = "retract", threads = 1, least_length= 300)
fd_obj <- analyze_curves_all_analytical_metrics(fd_obj, useCurve = "retract") 
md <- fd_obj@metadata
surface <- sub("_.*", "", md$filename)
surface[surface == "F"] <- "Group_A"
surface[surface == "P"] <- "Group_B"
surface[surface == "silicon"] <- "Group_C"
surface[surface == "Z"] <- "Group_D"
md$surface <- surface
fd_obj@metadata <- md
plot_metric_violin(df = fd_obj@metadata, metric_name = "adhesive_force_nN_retract",group_by = "surface",global_test = 'kruskal', pairwise_test = 'wilcox', p_adjust_method = 'BH')

}
