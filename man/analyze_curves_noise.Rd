% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curve_analysis.R
\name{analyze_curves_noise}
\alias{analyze_curves_noise}
\title{Noise-band Analysis (nN) for All Transformed Curves in an fdObj}
\usage{
analyze_curves_noise(
  fdObj,
  useCurve = c("retract", "approach"),
  threads = 1,
  baseline_span = "automatic",
  threshold_method = c("sd", "mad", "quantile", "fixed"),
  multiplier = 3,
  mad_constant = 1.4826,
  quantile_low = 0.25,
  quantile_high = 0.75,
  fixed_low = NULL,
  fixed_high = NULL
)
}
\arguments{
\item{fdObj}{An object of class \code{fdObj}.}

\item{useCurve}{Character; must be one of \code{c("retract", "approach")}.}

\item{threads}{Integer. Number of parallel workers (default \code{1}).}

\item{baseline_span}{Either a single integer >= 1, or the string \code{"automatic"}.
When \code{"automatic"}, per-curve baseline spans are read from
\code{fdObj@metadata$baseline_span_<useCurve>}.}

\item{threshold_method}{Character scalar controlling how baseline noise bands are estimated.
One of \code{c("sd", "mad", "quantile", "fixed")} (default \code{"sd"}).}

\item{multiplier}{Numeric >= 0 scaling multiplier for all methods. Default 3.}

\item{mad_constant}{Scaling for MAD to be SD-equivalent. Default 1.4826.}

\item{quantile_low}{Lower quantile probability for \code{"quantile"} method. Default 0.25.}

\item{quantile_high}{Upper quantile probability for \code{"quantile"} method. Default 0.75.}

\item{fixed_low}{Numeric lower band (nN) for \code{"fixed"} method. Default NULL.}

\item{fixed_high}{Numeric upper band (nN) for \code{"fixed"} method. Default NULL.}
}
\value{
The updated \code{fdObj} with two new metadata columns:
\code{noiseBand_low_nN_<dir>} and \code{noiseBand_high_nN_<dir>}.
}
\description{
Applies \code{analyze_a_curve_noise()} to each transformed curve in an
\code{fdObj} and stores per-curve lower/upper noise bands in metadata:
\itemize{
  \item \code{noiseBand_low_nN_<dir>}
  \item \code{noiseBand_high_nN_<dir>}
}
where \code{<dir>} is \code{"approach"} or \code{"retract"}.
}
\examples{
folder <- system.file("extdata", package = "curvana")
fd_obj <- createFdObjFromFolder(folder)
fd_obj <- transform_curves(fd_obj, spring_constant = 0.1, useCurve = "retract", threads = 1, least_length= 300)
fd_obj <- analyze_curves_noise(fd_obj, useCurve = "retract", threads = 1)
}
\seealso{
analyze_a_curve_noise
}
